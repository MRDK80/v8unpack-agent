# Участие в разработке

Спасибо за интерес к проекту! Этот документ объясняет, как предложить
исправление или новую функциональность.

## Требования

- Python 3.10 или новее
- Git

## Быстрый старт

```bash
# 1. Форкните репозиторий на GitHub, затем клонируйте свой форк
git clone https://github.com/<ваш-логин>/v8unpack-agent.git
cd v8unpack-agent

# 2. Установите пакет в режиме разработки вместе с зависимостями для тестов
pip install -e ".[test]"

# 3. Убедитесь, что тесты проходят
pytest
```

## Рабочий процесс

1. Создайте ветку от `main` с говорящим именем:
   ```bash
   git checkout -b fix/form-artifact-warnings   # для исправлений
   git checkout -b feat/erf-batch-unpack         # для новых функций
   git checkout -b docs/update-readme            # для документации
   ```
2. Внесите изменения. Если добавляете новое поведение — добавьте тест.
3. Убедитесь, что тесты зелёные: `pytest`
4. Запушьте ветку и откройте Pull Request в `main`.

## Соглашение об именах веток

| Префикс | Когда использовать |
|---|---|
| `fix/` | Исправление бага |
| `feat/` | Новая функциональность |
| `docs/` | Только документация |
| `refactor/` | Рефакторинг без изменения поведения |
| `test/` | Только тесты |

## Соглашение о коммитах

Используйте префиксы в стиле [Conventional Commits](https://www.conventionalcommits.org/):

```
fix: корректно обрабатывать Form.bin с пустым Items/
feat: добавить пакетную распаковку .erf
docs: обновить примеры в README
test: покрыть is_form_stale граничными случаями
refactor: вынести логику mtime в отдельный модуль
```

## Требования к Pull Request

- Все 4 CI-джобы должны быть зелёными (ubuntu + windows, py3.10 + py3.12).
- Новая публичная функция — значит новый тест. Тесты синтетические: реальный
  контейнер 1С не нужен, используйте временные файловые деревья и
  заглушку-распаковщик (см. существующие тесты в `tests/`).
- Описание PR объясняет *что* изменилось и *почему*.
- Если PR меняет контракт публичной функции (состав или семантику полей
  в результате), это указывается в описании явно, а удалённый тест
  заменяется тестом на новое поведение, а не просто убирается.

## Классификация: неопределённость лучше догадки

Если данных для решения недостаточно, возвращайте `unknown`, а не
«безопасный дефолт». Догадка выглядит как результат и скрывает проблему.

Показательный случай из issue #98. Формы с пустым `tree` в `.elem.json`
предлагалось считать сервисными: «форма без элементов не может быть
объектной». Верификация на 2231 форме опровергла рассуждение — среди них
оказались `ФормаЗаписи` регистров сведений и основные формы отчётов.
Правило дало бы 1873 сервисных формы вместо 1793 и вычеркнуло десять
объектных форм из метрики покрытия, не оставив следа в отчёте.

Практические правила:

- Не расширяйте словари паттернов широкими префиксами ради полноты
  покрытия — проверьте каждый префикс на реальных данных.
- Возвращайте пару «класс, причина», если решение неочевидно: код причины
  позволяет отличить обоснованное решение от заглушки.
- Помечайте диагностические причины, требующие внимания, явным маркером
  в выводе верификатора.

## Диагностические причины: enum вместо строки

Когда функция возвращает `unknown`, следующий вопрос — «почему». Свободный
текст в `warnings` на этот вопрос не отвечает: его нельзя посчитать,
сгруппировать и отследить между прогонами. Причина должна быть машиночитаемой.

Показательный случай из issue #105. После #103 оставались 77 форм
с `elem_index_ok=False`, и все они давали одно предупреждение —
«Элементы формы не найдены». Отличить норму (печатная форма без виджетов
данных) от реального пробела декодера было невозможно. Введение
`UnindexedReason` разложило те же 77 форм на категории: 17 форм без виджетов
данных (норма), 48 с непопадающими UUID колонок `TabularField` (вероятный
пробел), 12 с пустой картой реквизитов владельца. Из одной строки «непонятно»
получились три разных тикета.

Правила для диагностических классификаторов:

- Причина — значение `Enum`, а не строка. Строка допустима только как
  человекочитаемое дополнение в поле `detail`.
- В enum обязательно есть значение `UNKNOWN` — оно означает «сценарий
  не покрыт», и его ненулевое количество на production-прогоне является
  поводом для отдельного разбора, а не нормой.
- Порядок проверок фиксируется явно и документируется таблицей: первое
  совпадение выигрывает. Иначе одна и та же форма получает разные причины
  при рефакторинге.
- Диагностическая функция ничего не создаёт и не изменяет: она не добавляет
  элементов, не заполняет `data_path` и не мутирует переданный результат.
  Проверьте это отдельным тестом-инвариантом.
- Классификатор — best-effort: внешняя обёртка перехватывает любые исключения
  и возвращает `UNKNOWN` с диагностикой. Вызов на несуществующей директории
  обязан не падать.
- Тесты организуются по одному классу на категорию плюс класс инвариантов
  (`TestInvariants`): отсутствие исключений, неизменность входа, полнота
  покрытия enum.

Отдельно про фикстуры: синтетический файл должен воспроизводить тот же
layout, что читает production-код. В #105 фикстура категории B изначально
писала `Catalog.json` без секции `header`, декодер возвращал ошибку, карта
реквизитов оказывалась пустой — и тест падал, требуя категорию B там, где
реализация правильно давала A. Красный тест указывал на фикстуру, а не на код.
Прежде чем править реализацию под тест, убедитесь, что фикстура валидна.

## Верификация на production-данных

Юнит-тесты не заменяют прогон на реальной выгрузке. Для изменений,
влияющих на классификацию или метрики, приложите к PR текстовый вывод
локальной верификации: распределение по категориям, проверку баланса
и частоты диагностических причин.

Скрипты verify_* не коммитятся — они привязаны к конкретной выгрузке
и без неё не запускаются. Соответствующее правило есть в .gitignore.

Верификация должна:

- обходить весь корпус, а не выборку;
- проверять баланс: сумма всех категорий равна числу найденных директорий;
- отдельно выводить потенциальные ложноположительные срабатывания;
- показывать частотное распределение причин, а не только итоговые счётчики;
- сверять распределение с ожидаемым: резкий сдвиг между категориями при
  «безопасном» рефакторинге — признак регресса, даже если сумма сошлась;
- не падать на отдельной форме — ошибки собираются в список и печатаются
  сводкой.

Перед коммитом:

```bash
pytest -q
```

## Запуск тестов

```bash
# Все тесты
pytest

# С подробным выводом
pytest -v

# Конкретный файл
pytest tests/test_form_artifact.py
```

## Вопросы

Если что-то непонятно — откройте [issue](https://github.com/MRDK80/v8unpack-agent/issues)
с описанием вопроса. Статья, которая стоит за этим пакетом:
[Обычные формы 1С в агентном пайплайне](https://infostart.ru/1c/articles/2721726/).
